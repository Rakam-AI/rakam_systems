[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai_agents"
version = "0.1.0"
description = "Agent-side components"
requires-python = ">=3.10"
dependencies = []
