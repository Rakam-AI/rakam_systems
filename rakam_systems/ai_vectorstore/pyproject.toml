[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai_vectorstore"
version = "0.1.0"
description = "Vector store and data components"
requires-python = ">=3.10"
dependencies = []
